[workspace]

members = [
    "read-fonts",
    "font-types",
    "font-codegen",
    "font-test-data",
    "write-fonts",
    "otexplorer",
    "skrifa",
]

[workspace.metadata.release]
allow-branch = ["main"]

[profile.release]
lto = true
codegen-units = 1
